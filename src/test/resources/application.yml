server:
  port: 8080

build.name: "abctechapi"
build.version: "0.0.2"

spring:
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
        username: sa
        password: sa